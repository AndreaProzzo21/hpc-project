**Objective & Structure:** In this fase we will start use NumPy and SciPy vectorization and MPI communication (mpi4py). We can still use jupyter to integrate graphs and theory concepts, but standard code will be executed as normal via terminal (since it start to be more complex).
